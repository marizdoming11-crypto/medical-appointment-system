@extends('layouts.app')@section('content')<div class="card"><div class="card-body"><h2>Edit Appointment</h2>@if(!$appointment)<div class="alert alert-danger">Appointment not found.</div>@else<form method="POST" action="/user/appointments/edit/{{ $appointment['id'] }}">@csrf<div class="mb-3"><label>Doctor</label><select name="doctorId" class="form-select" required>@foreach($doctors as $d)<option value="{{ $d['id'] }}" @if($appointment['doctorId']==$d['id']) selected @endif>{{ $d['fullName'] }} - {{ $d['specialization'] }}</option>@endforeach</select></div><div class="mb-3"><label>Date</label><input type="date" name="appointmentDate" value="{{ substr($appointment['appointmentDate'],0,10) }}" class="form-control" required></div><div class="mb-3"><label>Reason</label><textarea name="reason" class="form-control" required>{{ $appointment['reason'] }}</textarea></div><button class="btn btn-warning">Update</button><a href="/user/appointments" class="btn btn-secondary">Back</a></form>@endif</div></div>@endsection
